'use strict';

var expect = require('chai').expect;
var keycloakCheck = require('../index');

describe('keycloakCheck', function() {
    it('should allow valid url redirects', function() {
        // var result = keycloakCheck.auth(1);
        // expect(result).to.equal('1');
    });
});